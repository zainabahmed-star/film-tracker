<%- include('../partials/nav.ejs') %>

<h1>What I've Seen</h1>

<ul>
    <% movies.forEach(item => { %>

        <li>
            <a href="/movies/<%= item._id %>">
                <img src="<%= item.image.url %>" width="100">
                <h3><%= item.title %></h3>
            </a>
        </li>

   <% }) %>
</ul>